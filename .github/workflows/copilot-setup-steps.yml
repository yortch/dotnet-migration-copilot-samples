name: "Copilot Setup Steps"
on:
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/copilot-setup-steps.yml
jobs:
  copilot-setup-steps:
    # set runs-on to 'windows-latest' or 'ubuntu-latest' based on your needs
    runs-on: windows-latest
    permissions:
      contents: read
    steps:
      - uses: dotnet/modernize-dotnet-actions@upgrade-agent-actions
        with:
          dotnet-version: '10.0.x'
          #set 'true' to install .NET Framework SDK and targeting packs (4.7.1 – 4.8.1) on Windows runners, required for .NET Framework or desktop workloads
          install-framework-targeting-packs: 'true'
